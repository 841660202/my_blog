pipeline {
    agent any
    stages {
        stage('安装') {
            steps {
                sh 'docker -v'
                sh 'node -v'
                sh 'npm -v'
            }
        }
    }
}
